#include "iFileOpen.h"

namespace command
{
	bool iFileOpen::validate(ArgumentList&& args)
	{
		return true;
	}


	bool iFileOpen::execute(ArgumentList&& args)
	{
		return true;
	}
} // namespace command
